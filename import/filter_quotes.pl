#!/usr/bin/env perl

#---------------------------------------------------

while (<>) {
    if (my ($m) = m|'(.*?(?<!\\))'|) {
	my $o=$m;
	$m =~ s|"|""|g;
	$m =~ s|\\||g;
	s|\Q'$o'\E|"$m"|;
    }
    print;
}
