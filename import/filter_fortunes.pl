#!/usr/bin/env perl

$/="\n%\n";
while(<>){
    $_ =~ s/\n%\n//;
    $_ =~ s/\s+--\s*((?:\w+ )?(?:\w\. )*\w+(?: \w+)?)[^\n]*$/ -$1/;
    $_ =~ s/\n/ /g;
    $_ =~ s/\s+/ /g;
    if (length($_) <= 128) {
	print $_."\n";
    }
}
